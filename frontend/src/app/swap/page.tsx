import PageLayout from "@/components/Layout/PageLayout";
import SwapCard from "@/features/swap/SwapCard/SwapCard";

const SwapPage = () => {
  return (
    <PageLayout className="my-16 flex h-fit justify-center">
      <SwapCard />
    </PageLayout>
  );
};

export default SwapPage;
